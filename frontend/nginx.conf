server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta cae en index.html (routing del lado del cliente)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
